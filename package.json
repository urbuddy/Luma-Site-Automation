{
  "name": "lumajest",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "HEADLESS=true RETRIES=2 jest --config ./jest.config.js",
    "test1": "HEADLESS=false RETRIES=2 jest --config ./jest.config.js"
  },
  "devDependencies": {
    "jest": "^29.4.0",
    "jest-html-reporter": "^3.7.0",
    "jest-puppeteer": "^6.2.0",
    "jest-retries": "^1.0.1",
    "puppeteer": "^19.6.0"
  }
}
